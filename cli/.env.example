# ============================================================================
# Ammplify CLI Configuration
# ============================================================================
# Copy this file to .env and fill in the values.
#   cp .env.example .env

# ----------------------------------------------------------------------------
# RPC & Chain (Required for ALL commands)
# ----------------------------------------------------------------------------

# JSON-RPC endpoint for on-chain reads and writes
AMMPLIFY_RPC_URL=https://testnet-rpc.monad.xyz

# Chain ID of the target network
AMMPLIFY_CHAIN_ID=10143

# ----------------------------------------------------------------------------
# Private Key (Required for WRITE operations only)
# ----------------------------------------------------------------------------
# Needed by: maker (open/close/adjust/collect-fees/add-permission/remove-permission),
#            taker (open/close/collateralize/withdraw), token approve
# NOT needed by: pool, view, admin, token balance

AMMPLIFY_PRIVATE_KEY=0x...

# ----------------------------------------------------------------------------
# Middleware API (Optional)
# ----------------------------------------------------------------------------
# The hosted middleware provides richer query data for:
#   pool list, view positions, view taker-positions, view tvl,
#   view prices, view leaderboard, pool liquidity
# Defaults to https://api.ammplify.xyz if not set.

AMMPLIFY_MIDDLEWARE_URL=https://api.ammplify.xyz

# ----------------------------------------------------------------------------
# Deployed Addresses (Optional)
# ----------------------------------------------------------------------------
# Path to the deployed-addresses.json file containing contract addresses.
# Resolves relative to the cli/ directory (not the current working directory).
# Defaults to ../deployed-addresses.json (the repo root).

AMMPLIFY_ADDRESSES_FILE=../deployed-addresses.json
